apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsimate-chart.fullname" . }}
  labels:
    {{- include "opsimate-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsimate-chart.selectorLabels" . | nindent 4 }}
